# Hotel Guest App — Extracted Figma Assets

Assets extracted from `hotel_guest_app.fig`.

Image assets extracted: 13

The original hashed filenames are preserved so the assets remain traceable to the Figma package.
